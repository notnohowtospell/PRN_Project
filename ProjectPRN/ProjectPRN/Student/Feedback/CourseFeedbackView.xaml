<UserControl x:Class="ProjectPRN.Student.Feedback.CourseFeedbackView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Background="{DynamicResource MaterialDesignPaper}"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}">

    <UserControl.Resources>
        <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}">
            <Setter Property="Foreground" Value="{DynamicResource MaterialDesignColumnHeader}"/>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <!-- Header -->
        <Border Grid.Row="0" Background="{DynamicResource PrimaryHueMidBrush}" Padding="20">
            <StackPanel>
                <TextBlock Text="Danh Gia Khoa Hoc"
                           Style="{DynamicResource MaterialDesignHeadline5TextBlock}"
                           Foreground="{DynamicResource PrimaryTextBrush}" HorizontalAlignment="Center"/>
                <TextBlock Text="Chon khoa hoc can danh gia"
                           Foreground="{DynamicResource PrimaryTextBrush}" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                <TextBlock x:Name="txtStatus" Text="San sang"
                           Foreground="{DynamicResource PrimaryTextBrush}" HorizontalAlignment="Center" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>
        <!-- Search and Filter -->
        <Border Grid.Row="1" Background="{DynamicResource MaterialDesignCardBackground}" Padding="20" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="txtSearch" Grid.Column="0"
                         materialDesign:HintAssist.Hint="Tim kiem khoa hoc..."
                         Style="{DynamicResource MaterialDesignOutlinedTextBox}"
                         Margin="0,0,10,0" TextChanged="TxtSearch_TextChanged"/>
            </Grid>
        </Border>
        <!-- Course List -->
        <DataGrid x:Name="dgCourses" Grid.Row="2"
                   AutoGenerateColumns="False"
                   CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  SelectionMode="Single"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="All"
                  Margin="20"
                  MouseDoubleClick="DgCourses_MouseDoubleClick"
                  Background="{DynamicResource MaterialDesignCardBackground}"
                  Foreground="{DynamicResource MaterialDesignBody}">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Tên khoá học" Binding="{Binding CourseName}" Width="250" IsReadOnly="True"/>
                <DataGridTextColumn Header="Ngày đăng ký" Binding="{Binding EnrollmentDateText}" Width="120" IsReadOnly="True"/>
                <DataGridTextColumn Header="Trạng thái" Binding="{Binding CompletionStatusText}" Width="140" IsReadOnly="True"/>
                <DataGridTextColumn Header="Đánh giá hiện tại" Binding="{Binding CurrentRatingText}" Width="120" IsReadOnly="True"/>
                <DataGridTemplateColumn Header="Thao tác" Width="180">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Đánh giá"
                                        Style="{DynamicResource MaterialDesignFlatButton}"
                                        Tag="{Binding CourseId}"
                                        Click="BtnFeedback_Click">
                                    <Button.ToolTip>
                                        <TextBlock Text="{Binding FeedbackButtonTooltip}" Foreground="{DynamicResource MaterialDesignBody}"/>
                                    </Button.ToolTip>
                                </Button>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        <!-- Status Bar -->
        <Border Grid.Row="3" Background="{DynamicResource StatusBarBackgroundBrush}" Padding="20,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="txtRecordCount" Grid.Column="0"
                           Text="Tong so khoa hoc: 0" VerticalAlignment="Center"
                           Foreground="{DynamicResource MaterialDesignBody}"/>
                <TextBlock x:Name="txtLastUpdated" Grid.Column="1"
                           Text="Cap nhat: --/--/---- --:--:--" VerticalAlignment="Center"
                           Foreground="{DynamicResource MaterialDesignBodyLight}"/>
            </Grid>
        </Border>
        <!-- Loading Overlay -->
        <Border x:Name="LoadingOverlay" Grid.RowSpan="4"
                Background="#80000000" Visibility="Collapsed">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar Style="{DynamicResource MaterialDesignCircularProgressBar}"
                            Value="0" IsIndeterminate="True" Width="50" Height="50"/>
                <TextBlock Text="Dang tai du lieu..." Foreground="{DynamicResource PrimaryTextBrush}"
                           HorizontalAlignment="Center" Margin="0,10,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
