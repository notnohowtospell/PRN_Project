<Window x:Class="ProjectPRN.Student.Courses.EnrolledCoursesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Khóa học đã đăng ký" Height="400" Width="800"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        FontFamily="{DynamicResource MaterialDesignFont}">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <!-- Style for DataGrid Column Headers -->
        <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}">
            <Setter Property="Foreground" Value="{DynamicResource MaterialDesignColumnHeader}"/>
        </Style>
    </Window.Resources>
    <Grid Margin="10">
        <TextBlock x:Name="txtNoCourses"
               Text="Chưa có khóa học nào đã đăng ký."
               FontStyle="Italic"
               Foreground="{DynamicResource MaterialDesignBodyLight}"
               FontSize="14"
               Visibility="Collapsed"
               HorizontalAlignment="Center"
               VerticalAlignment="Center"
               TextAlignment="Center" />
        <DataGrid x:Name="dgEnrolledCourses"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  Background="{DynamicResource MaterialDesignCardBackground}"
                  Foreground="{DynamicResource MaterialDesignBody}"
                  GridLinesVisibility="Horizontal"
                  HorizontalGridLinesBrush="{DynamicResource MaterialDesignDivider}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Tên khóa học" Binding="{Binding CourseName}" Width="*" />
                <DataGridTextColumn Header="Giảng viên" Binding="{Binding InstructorName}" Width="150" />
                <DataGridTextColumn Header="Ngày bắt đầu" Binding="{Binding StartDate, StringFormat=\{0:dd/MM/yyyy\}}" Width="120" />
                <DataGridTextColumn Header="Ngày kết thúc" Binding="{Binding EndDate, StringFormat=\{0:dd/MM/yyyy\}}" Width="120" />
                <DataGridTextColumn Header="Giá" Binding="{Binding Price}" Width="100" />
                <DataGridTextColumn Header="Trạng thái khóa học" Binding="{Binding Status}" Width="150" />
                <DataGridTextColumn Header="Trạng thái đăng ký" Binding="{Binding PaymentStatusDisplay}" Width="150" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
