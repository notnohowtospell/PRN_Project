<Window x:Class="ProjectPRN.CourseMaterialManagement.CourseMaterialInputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProjectPRN.CourseMaterialManagement"
        mc:Ignorable="d"
        Title="Tài liệu khóa học" Height="300" Width="400"
WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Khóa học:" Grid.Row="0" VerticalAlignment="Center"/>
        <ComboBox x:Name="CourseComboBox" Grid.Row="0" Margin="100,0,0,5"
              DisplayMemberPath="CourseName" SelectedValuePath="CourseId"/>

        <TextBlock Text="Tiêu đề:" Grid.Row="1" VerticalAlignment="Center"/>
        <TextBox x:Name="TitleTextBox" Grid.Row="1" Margin="100,0,0,5"/>

        <TextBlock Text="Tệp:" Grid.Row="2" VerticalAlignment="Center"/>
        <StackPanel Grid.Row="2" Margin="100,0,0,5" Orientation="Horizontal">
            <TextBox x:Name="FilePathTextBox" Width="180" IsReadOnly="True"/>
            <Button Content="Chọn" Width="80" Margin="5,0,0,0" Click="ChooseFile_Click"/>
        </StackPanel>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Lưu" Width="80" Margin="0,0,10,0" Click="Save_Click"/>
            <Button Content="Hủy" Width="80" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
