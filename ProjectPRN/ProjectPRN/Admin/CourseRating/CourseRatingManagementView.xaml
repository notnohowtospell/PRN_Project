<UserControl x:Class="ProjectPRN.Admin.CourseRating.CourseRatingManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{DynamicResource PrimaryHueMidBrush}" Padding="20">
            <StackPanel>
                <TextBlock Text="Quan Ly Danh Gia Khoa Hoc" 
                          Style="{DynamicResource MaterialDesignHeadline5TextBlock}"
                          Foreground="White" HorizontalAlignment="Center"/>
                <TextBlock Text="Xem danh gia trung binh cua tat ca khoa hoc" 
                          Foreground="White" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                <TextBlock x:Name="txtStatus" Text="San sang" 
                          Foreground="White" HorizontalAlignment="Center" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- Search and Filter -->
        <Border Grid.Row="1" Background="White" Padding="20" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox x:Name="txtSearch" Grid.Column="0"
                         materialDesign:HintAssist.Hint="Tim kiem khoa hoc..."
                         Style="{DynamicResource MaterialDesignOutlinedTextBox}"
                         Margin="0,0,10,0" TextChanged="TxtSearch_TextChanged"/>



 
            </Grid>
        </Border>

        <!-- Course Rating List -->
        <DataGrid x:Name="dgCourseRatings" Grid.Row="2" 
                  AutoGenerateColumns="False" 
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  SelectionMode="Single"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="All"
                  Margin="20"
                  MouseDoubleClick="DgCourseRatings_MouseDoubleClick">
            
            <DataGrid.Columns>
                <DataGridTextColumn Header="Tên khoá" Binding="{Binding CourseName}" Width="250" IsReadOnly="True"/>
                <DataGridTextColumn Header="Số học viên" Binding="{Binding TotalStudents}" Width="100" IsReadOnly="True"/>
                <DataGridTextColumn Header="Số đánh giá" Binding="{Binding TotalRatings}" Width="100" IsReadOnly="True"/>
                <DataGridTextColumn Header="Đánh giá TB" Binding="{Binding AverageRatingText}" Width="120" IsReadOnly="True"/>
                <DataGridTextColumn Header="Ngày tạo" Binding="{Binding CreatedDateText}" Width="120" IsReadOnly="True"/>
                
                <DataGridTemplateColumn Header="Thao tác" Width="200">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Xem đánh giá" 
                                        Style="{DynamicResource MaterialDesignOutlinedButton}"
                                        Margin="2"
                                        Tag="{Binding CourseId}"
                                        Click="BtnViewFeedbacks_Click"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Status Bar -->
        <Border Grid.Row="3" Background="#F5F5F5" Padding="20,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock x:Name="txtRecordCount" Grid.Column="0" 
                          Text="Tong so khoa hoc: 0" VerticalAlignment="Center"/>
                
                <TextBlock x:Name="txtAverageStats" Grid.Column="1" 
                          Text="Danh gia TB toan he thong: 0.0/5" VerticalAlignment="Center" Margin="20,0"/>
                
                <TextBlock x:Name="txtLastUpdated" Grid.Column="2" 
                          Text="Cap nhat: --/--/---- --:--:--" VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- Loading Overlay -->
        <Border x:Name="LoadingOverlay" Grid.RowSpan="4" 
                Background="#80000000" Visibility="Collapsed">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar Style="{DynamicResource MaterialDesignCircularProgressBar}"
                            Value="0" IsIndeterminate="True" Width="50" Height="50"/>
                <TextBlock Text="Dang tai du lieu..." Foreground="White" 
                          HorizontalAlignment="Center" Margin="0,10,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>