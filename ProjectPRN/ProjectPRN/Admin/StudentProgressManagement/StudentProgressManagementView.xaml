<UserControl x:Class="ProjectPRN.Admin.StudentProgressManagement.StudentProgressManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{DynamicResource PrimaryHueMidBrush}" Padding="20">
            <StackPanel>
                <TextBlock Text="Quản Lý Tiến Trình Học Sinh" 
                          Style="{DynamicResource MaterialDesignHeadline5TextBlock}"
                          Foreground="White" HorizontalAlignment="Center"/>
                <TextBlock x:Name="txtStatus" Text="Sẵn sàng" 
                          Foreground="White" HorizontalAlignment="Center" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- Search and Filter -->
        <Border Grid.Row="1" Background="White" Padding="20" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox x:Name="txtSearch" Grid.Column="0"
                         materialDesign:HintAssist.Hint="Tìm kiếm học sinh (tên, mã số, email)"
                         Style="{DynamicResource MaterialDesignOutlinedTextBox}"
                         Margin="0,0,10,0" TextChanged="TxtSearch_TextChanged"/>

                <Button x:Name="btnRefresh" Grid.Column="1" Content="Làm mới" 
                        Style="{DynamicResource MaterialDesignOutlinedButton}"
                        Margin="5,0" Click="BtnRefresh_Click"/>

            </Grid>
        </Border>

        <!-- Student List -->
        <DataGrid x:Name="dgStudents" Grid.Row="2" 
                  AutoGenerateColumns="False" 
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  SelectionMode="Single"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="All"
                  Margin="20"
                  MouseDoubleClick="DgStudents_MouseDoubleClick">
            
            <DataGrid.Columns>
                <DataGridTextColumn Header="Mã SV" Binding="{Binding StudentCode}" Width="100" IsReadOnly="True"/>
                <DataGridTextColumn Header="Tên học sinh" Binding="{Binding StudentName}" Width="200" IsReadOnly="True"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="200" IsReadOnly="True"/>
                <DataGridTextColumn Header="Số khóa học" Binding="{Binding TotalCourses}" Width="100" IsReadOnly="True"/>
                <DataGridTextColumn Header="Hoàn thành" Binding="{Binding CompletedCourses}" Width="100" IsReadOnly="True"/>
                <DataGridTextColumn Header="Tiến độ tổng" Binding="{Binding OverallProgressText}" Width="120" IsReadOnly="True"/>
                <DataGridTextColumn Header="Trạng thái" Binding="{Binding Status}" Width="100" IsReadOnly="True"/>
                
                <DataGridTemplateColumn Header="Thao tác" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Xem chi tiết" 
                                        Style="{DynamicResource MaterialDesignFlatButton}"
                                        Margin="2"
                                        Tag="{Binding StudentId}"
                                        Click="BtnViewDetails_Click"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Status Bar -->
        <Border Grid.Row="3" Background="#F5F5F5" Padding="20,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock x:Name="txtRecordCount" Grid.Column="0" 
                          Text="Tổng số học sinh: 0" VerticalAlignment="Center"/>
                
                <TextBlock x:Name="txtLastUpdated" Grid.Column="1" 
                          Text="Cập nhật: --/--/---- --:--:--" VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- Loading Overlay -->
        <Border x:Name="LoadingOverlay" Grid.RowSpan="4" 
                Background="#80000000" Visibility="Collapsed">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar Style="{DynamicResource MaterialDesignCircularProgressBar}"
                            Value="0" IsIndeterminate="True" Width="50" Height="50"/>
                <TextBlock Text="Đang tải dữ liệu..." Foreground="White" 
                          HorizontalAlignment="Center" Margin="0,10,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>